import React from "react";
import ReactDOM from "react-dom";  // ✅ for React 17
import App from "./App";

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
